import { FC } from "react";
import { SynonymsBody } from "../../components";

type Props = {};

export const Synonyms: FC<Props> = (props) => {
  return (
    <div>
      <SynonymsBody />
    </div>
  );
};
