import { FC } from "react";
import { TranslateBody } from "../../components";

type Props = {};

export const Translate: FC<Props> = (props) => {
  return (
    <div>
      <TranslateBody />
    </div>
  );
};
